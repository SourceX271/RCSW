[project]
name = "rcsw"
version = "1.0.0"
description = "Remove CamScanner Watermark"
requires-python = ">=3.10"
dependencies = [
    "PySide6>=6.5",
    "PySide6-Fluent-Widgets>=1.5",
    "PyMuPDF>=1.23",
    "Pillow>=10.0",
]

[project.scripts]
rcsw = "rcsw.app:main"
